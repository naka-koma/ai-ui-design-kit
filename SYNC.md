# SYNC — 共通Kitの更新プロトコル

Kit Syncは中央Kitの共通Skillと指示を更新する操作。実装と設計文書のずれを調べる **Design Auditではない**。UIコードの修正や `docs/design/**` の更新を含めない。

## 1. ローカルmanifestを読む

対象ルートの `.ui-design-kit.yml` から取得元、導入済みversion/revision、所有区分を読む。[manifest仕様](docs/manifest.md)を参照。manifestがない場合は推測で更新せず、[SETUP](SETUP.md)で既存内容を調べて導入する。未知のschemaは移行仕様を確認するまで適用しない。

作業差分を確認し、更新対象の導入前状態とプロジェクト文書のハッシュ/差分を記録する。パスを正規化し、対象ルートの外、symlink経由の外部、所有範囲の重複を拒否する。`docs/design/**` はmanifestに誤ってmanagedと書かれていても保護する。

## 2. 取得元と更新先を確定

`source.repository` を使い、指定された版、なければ最新の安定リリースを一時取得する。未リリース運用で安定版がない場合だけ既定ブランチのコミットを固定して使用し、スナップショット更新と報告する。安定版導入済みなのにリリース情報が取れない場合、無断でブランチへ切り替えない。

導入済みrevisionと更新先の完全SHAを確認し、更新先の `SYNC.md`、manifest仕様、CHANGELOG、移行説明を読む。同一revisionなら変更なし。明示指定がないダウングレードはしない。取得失敗・版の不一致はローカル変更前に停止する。

## 3. 差分を準備

導入済みrevisionのソースとローカル管理ファイルを比較してローカル編集を検出する。旧版が取得できず編集有無を判断できない場合も、衝突として保持する。managedであっても検出したローカル編集を黙って破棄しない。変更箇所を提示し、プロジェクト独自ルールへの移設や明示的な置換で解決する。

| 所有区分 | 適用方法 |
| --- | --- |
| managed | 登録された共通SkillファイルとLICENSEを更新先の内容へ置換 |
| merge | 導入済みsnippet・ローカル・更新先の3者を比較し、Kitブロックを統合。ブロック外は保持 |
| project_owned | 読み取り確認のみ。テンプレートの新版があっても書き込まない |

managed内の旧版にあって新版で削除されたファイルは、旧版とローカルが一致するものだけ削除対象にする。未知の追加ファイルは保持して報告し、ディレクトリ丸ごとの削除は避ける。所有区分の追加・変更は移行仕様と実際の対象を確認し、保護先への拡張を許容しない。

## 4. 適用と検証

全差分を準備し、解決できない競合があれば適用前に停止する。復旧用に今回変更するファイルの内容を一時保存し、managedとmergeを適用する。ブロック重複、リンク切れ、欠落、配布元との不一致を確認する。`docs/design/**` と他のproject-owned領域がバイト単位で変わっていないことを確認する。

途中失敗時は今回の変更だけを復旧し、ユーザーの既存差分は保持する。全体のgit resetは使わない。復旧できない場合は部分適用の内容を正確に報告し、完了を主張しない。

## 5. 成功を記録

すべての検証が成功してからmanifestの `source.version` と `source.revision` を更新する。移行がない所有設定や追加フィールドは保持する。manifest書き込みも含めて失敗時の復旧対象とする。

導入前→更新後の版、更新/削除したmanagedファイル、統合した指示、保持したローカル追加、project-owned不変の確認、未解決事項を報告する。テンプレートの改善点は任意の別作業として紹介できるが、Syncで適用しない。
